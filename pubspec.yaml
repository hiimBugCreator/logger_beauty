name: logger_beauty
description: Transform your debug screens into beautiful, informative workspaces with this Flutter package. Supports DioInterceptor for effortless network request tracking.
version: 1.0.1
homepage: https://github.com/hiimBugCreator/logger_beauty/

environment:
  sdk: '>=3.1.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.3